package gov.nasa.arc.europa.constraintengine.test

import org.scalatest.FunSuite
import org.scalatest.Tag
import org.scalatest.matchers.ShouldMatchers

class EquivalenceClassTest extends FunSuite with ShouldMatchers { 
  ignore("BasicAllocation", Nope){ }
  ignore("ConstructionOfSingleGraph", Nope){ }
  ignore("SplittingOfSingleGraph", Nope){ }
  ignore("MultiGraphMerging", Nope){ }
  ignore("EqualityConstraintPropagator", Nope){ }
}
